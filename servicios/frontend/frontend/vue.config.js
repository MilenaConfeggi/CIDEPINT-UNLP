module.exports = {
    outputDir: '../dist',     // ⬅️ Vue compilará todo en esta carpeta
    assetsDir: 'static',      // ⬅️ Archivos como CSS, JS e imágenes van a /static
    publicPath: '/',          // ⬅️ Las rutas serán absolutas (importante para producción)
  }
  
  